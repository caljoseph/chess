package server.handler;

import spark.Request;
import spark.Response;

public class LoginHandler extends Handler {
    public static Object handleRequest(Request req, Response res) {
        //deserialize req to a LoginRequest
        return "";
    }
}
